import ConsentWizard from "@/components/demo/consent/ConsentWizard";

export const metadata = {
  title: "Capture Consent — ConsentHub Demo",
};

export default function ConsentPage() {
  return <ConsentWizard />;
}
